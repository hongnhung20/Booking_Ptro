const initState = {
    isLoggedIn : false,
    token: null
}
const userReducer = (state = initState,acction)=>{
    switch(acction.type){
        default:
            return state;
    }
}
export default userReducer